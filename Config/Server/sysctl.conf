# Reverse path filtering (anti-spoof) - costuma ser ok em host "normal"
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# Não aceitar source routing
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.default.accept_source_route = 0

# Não aceitar redirects ICMP (evita mudanças de rota via rede)
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0

# Não enviar redirects (host não deve "ensinar" rotas)
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0
# Ignorar ICMP bogus
net.ipv4.icmp_ignore_bogus_error_responses = 1

# Ignorar ping broadcast (Smurf)
net.ipv4.icmp_echo_ignore_broadcasts = 1
# SYN cookies (mitiga SYN flood)
net.ipv4.tcp_syncookies = 1

# Tolerância a seq/ack mais estritos (geralmente default já é ok)
net.ipv4.tcp_rfc1337 = 1

# Loga pacotes com endereço "impossível" (martians)
net.ipv4.conf.all.log_martians = 1
net.ipv4.conf.default.log_martians = 1

# Evita core dump de binário setuid (vazamento de segredo)
fs.suid_dumpable = 0

# Restringe ptrace (anti “process snooping”
kernel.yama.ptrace_scope = 1

# Endereços em /proc para não vazar info a usuários não-root
kernel.kptr_restrict = 2
kernel.dmesg_restrict = 1

fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# Restringe uso de eBPF a privilegiados (evita abuse local)
kernel.unprivileged_bpf_disabled = 1
